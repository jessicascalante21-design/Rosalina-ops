import { Link } from 'react-router-dom';
import { Headphones, ClipboardList, AlertTriangle, Info, ArrowRight } from 'lucide-react';
import { motion } from 'framer-motion';
import StatusIndicator from '@/components/StatusIndicator';

const actions = [
  { to: '/concierge', label: 'Get Help Now', desc: 'Connect with our live concierge team', icon: Headphones, accent: true },
  { to: '/request', label: 'Request Service', desc: 'Submit a maintenance or amenity request', icon: ClipboardList, accent: false },
  { to: '/pre-arrival', label: 'Pre-Arrival Check-In', desc: 'Complete your arrival information', icon: Info, accent: false },
  { to: '/emergency', label: 'Emergency', desc: 'Report an urgent safety issue', icon: AlertTriangle, accent: false },
];

const faqs = [
  { q: 'What are check-in / check-out times?', a: 'Check-in is at 3:00 PM and check-out is at 11:00 AM. Early check-in or late check-out may be available upon request.' },
  { q: 'Is there parking available?', a: 'Yes, complimentary parking is available on-site. Please note your car information in your pre-arrival form.' },
  { q: 'How do I access Wi-Fi?', a: 'Connect to "Rosalina-Guest" network. The password is provided in your welcome packet.' },
  { q: 'What amenities are included?', a: 'All stays include fresh linens, towels, coffee/tea station, and 24/7 concierge support during operating hours.' },
];

export default function Index() {
  return (
    <div className="min-h-screen">
      {/* Hero */}
      <section className="gradient-hero text-primary-foreground py-20 px-4">
        <div className="container max-w-4xl text-center">
          <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }}>
            <StatusIndicator />
            <h1 className="font-heading text-4xl md:text-5xl font-bold mt-6 mb-4">
              Real-time support for your stay
            </h1>
            <p className="text-lg md:text-xl opacity-80 max-w-2xl mx-auto mb-8">
              Your personal concierge hub — get help, request services, and access property information all in one place.
            </p>
            <Link
              to="/concierge"
              className="inline-flex items-center gap-2 px-6 py-3 rounded-lg gradient-primary font-heading font-semibold text-primary-foreground shadow-lg hover:shadow-xl transition-shadow"
            >
              <Headphones size={20} />
              Connect with Concierge
              <ArrowRight size={16} />
            </Link>
          </motion.div>
        </div>
      </section>

      {/* Actions Grid */}
      <section className="container py-16">
        <h2 className="font-heading text-2xl font-bold mb-8">How can we help?</h2>
        <div className="grid sm:grid-cols-2 gap-4">
          {actions.map((a, i) => (
            <motion.div
              key={a.to}
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: i * 0.1, duration: 0.4 }}
            >
              <Link
                to={a.to}
                className={`group flex items-start gap-4 p-5 rounded-xl border transition-all ${
                  a.accent
                    ? 'border-primary/30 bg-primary/5 hover:bg-primary/10'
                    : 'border-border bg-card hover:border-primary/30 hover:shadow-sm'
                }`}
              >
                <div className={`w-10 h-10 rounded-lg flex items-center justify-center shrink-0 ${
                  a.accent ? 'gradient-primary text-primary-foreground' : 'bg-accent text-foreground'
                }`}>
                  <a.icon size={20} />
                </div>
                <div>
                  <p className="font-heading font-semibold text-foreground group-hover:text-primary transition-colors">{a.label}</p>
                  <p className="text-sm text-muted-foreground mt-0.5">{a.desc}</p>
                </div>
                <ArrowRight size={16} className="ml-auto mt-1 text-muted-foreground group-hover:text-primary transition-colors" />
              </Link>
            </motion.div>
          ))}
        </div>
      </section>

      {/* FAQ */}
      <section className="container pb-20">
        <h2 className="font-heading text-2xl font-bold mb-8">House Info & FAQ</h2>
        <div className="grid md:grid-cols-2 gap-4">
          {faqs.map((f, i) => (
            <div key={i} className="bg-card border border-border rounded-xl p-5">
              <h3 className="font-heading font-semibold text-foreground mb-2">{f.q}</h3>
              <p className="text-sm text-muted-foreground leading-relaxed">{f.a}</p>
            </div>
          ))}
        </div>
      </section>
    </div>
  );
}
